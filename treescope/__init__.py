"""treescope -- a `tree`-like directory viewer for the command line.

Pure Python standard library, no dependencies. Prints a directory tree with
ASCII or Unicode connector glyphs, optional per-directory size summaries,
`.gitignore`-style ignore patterns, sort modes, and directory-only mode.
"""

__version__ = "1.0.0"